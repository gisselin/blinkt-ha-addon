#include <tunables/global>

profile pimoroni_blinkt flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  file,
  network,
  signal (send) set=(kill,term,int,hup,cont),

  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /usr/local/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /usr/lib/bashio/** ix,
  /run/{,**} rwk,
  /tmp/** rwk,
  /data/** rwk,
  /app/** rix,

  /dev/tty rw,
  /dev/gpiochip[0-9]* rw,
  /sys/class/gpio/{,**} r,
  /sys/bus/gpio/devices/{,**} r,
  /device-tree/{,**} r,
}
